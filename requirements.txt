# Requirements for the posture detection Python script
# Use Python 3.8 - 3.11 for best compatibility with mediapipe

# MediaPipe requires numpy < 2 (as of mediapipe 0.10.x)
numpy<2,>=1.21

# MediaPipe (pose detection)
mediapipe==0.10.21

# OpenCV for camera and visualization
opencv-python>=4.5.5

# Optional: if running on a headless server (no GUI), use this instead of opencv-python
# opencv-python-headless

# WSGI server for deployment (e.g. Render)
gunicorn>=20.1.0

# Web framework for the WSGI app
Flask>=2.0,<3.0

# Add any other project-specific dependencies here
